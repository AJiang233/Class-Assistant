# 文案规范

面向用户的中文文案（页面标题、按钮、表单标签、提示语、空态、浏览器标题、通知渠道名…）的统一口径。

新增或改动文案前对照本表；发现新的不一致，**直接改到这张表里**——表是活的，不是摆设。

适用范围：`web/`（HTML、`assets/js`，以及 `backend/src` 里会返回给用户的提示语）、`android/`。
各端之间也要对齐：同一个功能在网页与 App 上应该是同一个说法。

## 1. 术语表：同一个东西只有一个叫法

| 概念 | 统一叫法 | 不要写成 |
| --- | --- | --- |
| 班级通知模块 | 班级通知 | 通知、通知中心 |
| 班级活动模块 | 班级活动 | 活动、活动中心 |
| 课表 + 学业达成模块 | 学业 | 教务、课程表、课表与学业 |
| 学业达成那一块 | 学业达成 | 学分达成、学分看板 |
| 课表里没排时间的课 | 未安排课程 | 未排课程 |
| 待填写的表单 | 表单 / 待填表单 | 问卷、表格、新表单 |
| 一条内容的作者 | 发布人 | 通知人、创建人 |
| 会被提醒的人 | 提醒对象 | 被通知人、接收人、提交对象 |
| 班级里的身份（班长、团支书…） | 职位 | 职务、角色 |
| 通知 / 活动的有效期 | 截止时间 | 存活至、过期时间 |
| 手机 App 之外的那一端 | 网页版 | Web 端、浏览器版 |
| 教务那边发给我们的登录凭据 | 教务登录态（过期时说「已过期」） | 教务会话、认证会话、教务系统登录态 |
| 短信 / 邮箱验证码这一步 | 二次验证 | 多因子、双因子、认证 |
| 用户绑定在账号上的邮箱 | 邮箱（状态写「已验证 / 未验证」，未绑定写「未绑定」） | 邮件地址、邮件、绑定邮箱 |
| 填邮箱 → 收验证码 → 填码确认这件事 | 邮箱验证 | 邮箱认证、邮箱校验、邮箱绑定 |
| 忘记密码后靠邮箱重设 | 找回密码 | 密码找回、忘记密码（可作入口名，正文用「找回密码」） |
| 班级里的人 | 成员 | 用户、账号（指人时） |
| 推送测试的结果 | 已发送，请查看通知栏 | 已推送、发送成功 |

## 2. 按钮与动作

- **编辑类**一律「编辑」，不用「修改」——列表行、弹窗标题、提交按钮都统一
- **保存类**主按钮一律「保存」（改密码表单也是「保存」，表单标题已说明改的是什么）
- **查看入口**一律「查看全部」，不用「全部活动」「查看更多」
- **二次确认**用「确定…？」+「确定 / 取消」两个按钮，不用「关闭」「好的」
- **创建类按对象定动词**，且同一个对象在标题与按钮上必须同一个词：
  发布通知 / 发布活动 / 添加表单 / 添加成员 / 添加职位
  （反面例子：卡片标题写「添加成员」，按钮却写「注册成员」）
- **成功反馈**用「已 + 动作」，不要只写「成功」「更新成功」：
  已保存 / 成员已删除 / 职位已添加 / 表单已添加 / 通知已保存
- **邮箱类动作收在一个「管理」里**：资料卡那一行只给「邮箱 + 一个「管理」按钮」，验证状态徽章
  放在弹窗标题后（已验证 / 未验证；未绑定不显示），绑定 / 更换 / 解绑都进弹窗做 —— 三个动作
  各摊一个按钮的话，行上会随状态变出不同按钮，也让人找不到「要改邮箱去哪」的唯一入口；
  已验证的邮箱在弹窗里只读，不发码、不给验证码区（换邮箱先解绑再绑）
- **验证码类按钮**：「发送验证码」（进行态「发送中…」）；被 1 分钟限发拦下时说
  「验证码发送过于频繁，请稍后再试」，不要写成「请 60 秒后再试」这种逼用户计时的说法

## 3. 标点与格式

- 中文里的括号一律**全角**「（）」，错误提示里的状态码也放进去：`请求失败（500）`
- 并列用顿号「、」，不用半角斜杠串中文；**例外**：搜索框占位用「 / 」分隔检索维度，
  如 `搜索姓名 / 学号 / 职位`
- 分隔信息用「·」，如「发布人 张三 · 09-14 10:00」
- 省略号用「…」，不写三个点
- 需要用户输入半角符号的字段，提示里写明是「**英文**逗号」，不要只说「逗号」
- 中文单位不加空格（`2 分钟`）；不要中英单位混排（写 `30 秒`，不写 `30s`）

## 4. 状态与反馈

- **内容区占位**：「正在加载…」（统一走 `stateHTML('正在加载…')`）
- **按钮进行态**：「保存中…」「提交中…」——按钮上不写「正在…」（太长）
- **空态**要说清「为什么空」，不要只写「暂无数据」：
  - 「今天没有安排」
  - 「还没同步到课表。到「学业」绑定教务系统后会自动同步」
  - 「今天暂无安排」（今日活动为空，但那是正常状态，不是缺数据）
- **错误态**一句话说清「发生了什么 + 我该做什么」：
  - 「教务登录态已过期，当前显示的是上次同步的课表 · 课表有变动时请点「刷新」」
- **常态提醒不要用红色横幅**：教务登录态过期、缓存数据这类「不是错误、只是说明」的情况，
  用一行小字（`.ac-alert.ac-hint`），红底横幅留给真正出错的时候

## 5. 标题层级

- 浏览器 `<title>` 写成「页面名 - 班级助理」，页面名与页内 `h1`、侧栏 / 底部导航**完全一致**：
  `班级通知 - 班级助理`，而不是 `通知 - 班级助理`
- 例外：没有固定页名的页面（如 `forms.html`，页内标题是动态的表单名）用用途做页面名：
  「填写表单 - 班级助理」
- 页内 `h1` 是模块名（「班级通知」），区块标题是内容名（「正在进行」「已经结束」），两者别互换
- iframe 的 `title` 属性（无障碍读屏用）同样写模块名
- 原生端同理：通知渠道名用模块名（**班级活动提醒**，不是「活动提醒」）、小组件标题用「今日活动 / 今日课程」

## 6. 语气

- 说人话，不堆「请」；错误提示不指责用户（「学号或密码不正确」，不写「你输入错了」）
- 只有数据丢失、不可逆操作才用重语气，并在确认框里写明后果
  （「删除后这条通知及其提醒都会消失」）
- 不拿「系统」当主语推卸责任（内测横幅那种整站公告除外）

## 7. 错误提示：技术细节留在日志里

用户看到的错误只说「发生了什么 + 我该做什么」。下面这些**一律不进提示**，改写进日志（`console.error`）：

- 异常信息（`e.message`）、堆栈、HTTP 状态码
- 内部标识：环境变量名（`JWT_SECRET`）、表名（权限表）、内部字段（执行计划）、字段的英文 key（`q1`）、
  字段类型的英文名（`text` / `radio`）
- 上游返回的原文（可能含 HTML 片段）、登录流程的跳转链、Cookie 名
- 订阅地址 / 公钥 / 认证密钥这类实现细节

| ✗ 给用户看的 | ✓ 应该写成 |
| --- | --- |
| 无法连接教务系统：教务系统响应超时（>10s）：/api/xxx | 暂时连不上教务系统，请稍后重试 |
| 服务端未配置 JWT_SECRET | 服务端暂时不可用，请联系管理员 |
| 订阅地址必须是 https / 公钥不合法 | 这台设备的订阅信息不完整，请重新开启通知 |
| 开启失败：Registration failed - push service error | 这台设备连不上浏览器的推送服务，网页通知暂时开不了。换个网络或浏览器可以再试；其它功能不受影响。 |
| 字段标识重复：q1 | 字段标识重复了，请换一个 |

例外：用户**必须知道才能行动**的信息要留着，且说清去哪做
（「通知权限未开启，请到系统设置里允许「班级助理」发通知」）。

（第 7 节那条「技术细节改写日志」已经落实到 `backend/src`：原先直接拼给用户的异常、
上游原文、跳转链、Cookie 名、配置项名，现在都改成只进 `console.error`。）
