<?xml version="1.0" encoding="utf-8"?>
<!-- 今日活动小组件。活动行是 ListView（集合组件，数据见 EventsWidgetService）：今天几场活动就列几行，
     装不下往下滚 —— 卡片结构与课表**完全一致**（三行文字 + 左侧彩条），两块摆在一起才是同一套东西。

     行距**不能**靠 item 自己的 layout_margin：ListView 会忽略 item 根布局的 margin。
     这里用 5dp 的透明 divider 当行距，标题到第一行的那 5dp 由列表的 paddingTop 给。 -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="14dp"
    android:background="@drawable/widget_bg">

    <TextView
        android:id="@+id/widget_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/widget_title"
        android:textColor="#111114"
        android:textSize="13sp"
        android:textStyle="bold"
        android:maxLines="1"
        android:ellipsize="end" />

    <!-- height 0dp + weight 1：占满标题以下的全部高度，超出由列表自己滚 -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingTop="5dp"
        android:divider="@android:color/transparent"
        android:dividerHeight="5dp" />

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="@string/widget_empty"
        android:textColor="#8d8d98"
        android:textSize="13sp" />
</LinearLayout>
