# 复制为 .dev.vars 后供 wrangler pages dev 读取，切勿提交真实值
# JWT 签名密钥（生产环境用 `openssl rand -hex 32` 生成，配到 Pages Secrets）
JWT_SECRET=replace-with-a-long-random-string

# 调用私有 Worker class-assistant-private-api 用的共享令牌。
# 必须与那个 Worker 上的 INTERNAL_TOKEN 逐字相同，否则教务接口一律 401
# （生产环境两边都配在 Secrets 里，见 web/README.md 的教务一节）
INTERNAL_TOKEN=replace-with-the-same-value-as-the-worker

# 注意：教务会话 Cookie 的封存密钥 COOKIE_SECRET 不在这里。
# 它只被私有 Worker 使用（在那边 `wrangler secret put COOKIE_SECRET`），Pages 侧不再需要。
